# URL del bridge HTTP→COM su questa macchina Windows (non la VM cloud).
SOLIDWORKS_BRIDGE_URL=http://127.0.0.1:47821

# Opzionale. Preferire Impostazioni nell'app (localStorage). Non committare la chiave.
# OPENROUTER_API_KEY=
# OPENROUTER_MODEL=openai/gpt-4o-mini
